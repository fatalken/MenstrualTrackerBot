# Telegram бот для отслеживания менструального цикла

Telegram-бот, предназначенный для мужчин, которые хотят лучше понимать и поддерживать свою девушку в разные периоды её менструального цикла.

## 🎯 Функционал

- 📊 Отслеживание текущей фазы менструального цикла
- 🔔 Ежедневные уведомления о состоянии с рекомендациями
- 📚 Справочник фаз цикла с подробной информацией
- 👤 Профиль пользователя с настройками
- ⚙️ Гибкая настройка уведомлений (время, часовой пояс)
- 💡 Персонализированные рекомендации по поддержке партнерши

## 📋 Требования

- Python 3.8+
- Ubuntu Server (или другая Linux-система)
- Telegram аккаунт
- База данных (SQLite для разработки, PostgreSQL для продакшена)

## 🚀 Установка и настройка

### 1. Подготовка сервера Ubuntu

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip python3-venv -y

# Установка PostgreSQL (опционально, для продакшена)
sudo apt install postgresql postgresql-contrib -y
```

### 2. Создание бота в Telegram

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 3. Клонирование и настройка проекта

```bash
# Перейдите в директорию для проектов
cd ~
mkdir bots
cd bots

# Если используете git, клонируйте репозиторий
# git clone <your-repo-url> menstrual_tracker_bot
# cd menstrual_tracker_bot

# Или создайте директорию и скопируйте файлы
mkdir menstrual_tracker_bot
cd menstrual_tracker_bot
```

### 4. Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 5. Настройка конфигурации

```bash
# Создание файла .env из примера
cp .env.example .env

# Редактирование .env файла
nano .env
```

Заполните файл `.env`:

```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=sqlite:///menstrual_tracker.db
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_NOTIFICATION_TIME=09:00
```

### 6. Инициализация базы данных

При первом запуске бота база данных создастся автоматически. Но можно инициализировать вручную:

```bash
python3 -c "from database import init_db; init_db()"
```

### 7. Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запуск бота
python3 bot.py
```

Если все работает, остановите бота (Ctrl+C) и переходите к настройке автозапуска.

## 🔧 Настройка PostgreSQL (опционально, для продакшена)

Если хотите использовать PostgreSQL вместо SQLite:

```bash
# Создание базы данных
sudo -u postgres psql

# В консоли PostgreSQL:
CREATE DATABASE menstrual_tracker;
CREATE USER bot_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE menstrual_tracker TO bot_user;
\q

# Обновите DATABASE_URL в .env:
# DATABASE_URL=postgresql://bot_user:ваш_пароль@localhost:5432/menstrual_tracker
```

## 🔄 Настройка автозапуска (systemd)

Создайте systemd сервис для автоматического запуска бота:

```bash
sudo nano /etc/systemd/system/menstrual-tracker-bot.service
```

Вставьте следующее содержимое (замените пути на свои):

```ini
[Unit]
Description=Menstrual Tracker Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/bots/menstrual_tracker_bot
Environment="PATH=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin"
ExecStart=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin/python3 /home/ваш_пользователь/bots/menstrual_tracker_bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация сервиса:

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable menstrual-tracker-bot

# Запуск сервиса
sudo systemctl start menstrual-tracker-bot

# Проверка статуса
sudo systemctl status menstrual-tracker-bot

# Просмотр логов
sudo journalctl -u menstrual-tracker-bot -f
```

## 📝 Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Следуйте инструкциям для заполнения данных
4. Настройте уведомления в меню
5. Получайте ежедневные отчеты и рекомендации

## 🗂️ Структура проекта

```
menstrual_tracker_bot/
├── bot.py                 # Основной файл бота
├── database.py            # Модели базы данных
├── cycle_calculator.py    # Логика расчета фаз цикла
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (не в git)
├── .env.example          # Пример конфигурации
├── README.md             # Документация
└── venv/                 # Виртуальное окружение (не в git)
```

## 🔐 Безопасность

- **Никогда не публикуйте** файл `.env` с токеном бота
- Используйте сильные пароли для базы данных
- Регулярно обновляйте зависимости: `pip install --upgrade -r requirements.txt`
- Делайте резервные копии базы данных

## 🛠️ Обслуживание

### Резервное копирование базы данных

```bash
# Для SQLite
cp menstrual_tracker.db menstrual_tracker_backup_$(date +%Y%m%d).db

# Для PostgreSQL
pg_dump -U bot_user menstrual_tracker > backup_$(date +%Y%m%d).sql
```

### Обновление бота

```bash
# Остановка сервиса
sudo systemctl stop menstrual-tracker-bot

# Активация виртуального окружения
source venv/bin/activate

# Обновление кода (если используете git)
git pull

# Обновление зависимостей
pip install --upgrade -r requirements.txt

# Запуск сервиса
sudo systemctl start menstrual-tracker-bot
```

### Просмотр логов

```bash
# Логи systemd
sudo journalctl -u menstrual-tracker-bot -n 100 -f

# Или логи Python (если настроены)
tail -f bot.log
```

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте статус сервиса: `sudo systemctl status menstrual-tracker-bot`
2. Проверьте логи: `sudo journalctl -u menstrual-tracker-bot -n 50`
3. Убедитесь, что токен бота правильный в `.env`
4. Проверьте подключение к интернету

### Ошибки базы данных

1. Проверьте права доступа к файлу базы данных
2. Для PostgreSQL проверьте, что сервис запущен: `sudo systemctl status postgresql`
3. Проверьте подключение: `psql -U bot_user -d menstrual_tracker`

### Уведомления не приходят

1. Проверьте, что уведомления включены в профиле пользователя
2. Убедитесь, что время и часовой пояс настроены правильно
3. Проверьте логи на наличие ошибок

## 📚 Дополнительная информация

### Фазы менструального цикла

1. **Менструальная фаза** (дни 1-7): Кровотечение, низкая активность
2. **Фолликулярная фаза** (дни 7-14): Рост энергии, улучшение настроения
3. **Овуляция** (день 14): Пик активности, повышенное либидо
4. **Лютеиновая фаза** (дни 15-28): ПМС, эмоциональная нестабильность

### Технические детали

- Использует библиотеку `python-telegram-bot` версии 20.7
- База данных: SQLite (разработка) или PostgreSQL (продакшен)
- Асинхронная обработка сообщений
- Планировщик задач для уведомлений

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Поддержка

При возникновении проблем создайте issue в репозитории или обратитесь к разработчику.

---

**Важно:** Этот бот создан для поддержки и понимания. Помните о важности открытого общения с партнершей и уважении к её чувствам. 💕
