# 📘 Подробная инструкция по развертыванию на Ubuntu

## 🎯 Цель

Развернуть Telegram-бота для отслеживания менструального цикла на сервере Ubuntu с автоматическим запуском и работой в фоновом режиме.

## 📋 Предварительные требования

- Сервер Ubuntu 20.04 или новее
- Доступ по SSH с правами sudo
- Telegram аккаунт для создания бота

## 🔧 Шаг 1: Подготовка сервера

### 1.1 Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 1.2 Установка необходимых пакетов

```bash
# Python и инструменты разработки
sudo apt install python3 python3-pip python3-venv git -y

# PostgreSQL (опционально, для продакшена)
sudo apt install postgresql postgresql-contrib -y
```

## 🤖 Шаг 2: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Менструальный трекер")
4. Введите username бота (должен заканчиваться на `bot`, например: `menstrual_tracker_bot`)
5. Сохраните полученный токен - он понадобится позже

**Пример токена:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## 📁 Шаг 3: Установка проекта

### 3.1 Создание директории

```bash
cd ~
mkdir -p bots
cd bots
```

### 3.2 Копирование файлов проекта

Если у вас есть git репозиторий:
```bash
git clone <ваш-репозиторий> menstrual_tracker_bot
cd menstrual_tracker_bot
```

Или скопируйте файлы вручную через SCP/SFTP в директорию `~/bots/menstrual_tracker_bot/`

### 3.3 Структура файлов должна быть:

```
~/bots/menstrual_tracker_bot/
├── bot.py
├── database.py
├── cycle_calculator.py
├── config.py
├── requirements.txt
├── .env.example
├── README.md
└── deploy.sh
```

## ⚙️ Шаг 4: Настройка окружения

### 4.1 Создание виртуального окружения

```bash
cd ~/bots/menstrual_tracker_bot
python3 -m venv venv
source venv/bin/activate
```

### 4.2 Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4.3 Настройка конфигурации

```bash
# Создание .env файла
cp .env.example .env

# Редактирование .env
nano .env
```

Вставьте следующее содержимое (замените токен на свой):

```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=sqlite:///menstrual_tracker.db
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_NOTIFICATION_TIME=09:00
```

**Для PostgreSQL** (рекомендуется для продакшена):
```env
BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=postgresql://bot_user:пароль@localhost:5432/menstrual_tracker
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_NOTIFICATION_TIME=09:00
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

## 🗄️ Шаг 5: Настройка базы данных

### Вариант A: SQLite (простой, для начала)

База данных создастся автоматически при первом запуске. Можно инициализировать вручную:

```bash
source venv/bin/activate
python3 -c "from database import init_db; init_db()"
```

### Вариант B: PostgreSQL (рекомендуется для продакшена)

```bash
# Вход в PostgreSQL
sudo -u postgres psql

# Создание базы данных и пользователя
CREATE DATABASE menstrual_tracker;
CREATE USER bot_user WITH PASSWORD 'ваш_надежный_пароль';
GRANT ALL PRIVILEGES ON DATABASE menstrual_tracker TO bot_user;
ALTER DATABASE menstrual_tracker OWNER TO bot_user;
\q

# Обновите DATABASE_URL в .env файле
```

## 🧪 Шаг 6: Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запуск бота
python3 bot.py
```

Вы должны увидеть сообщение: "Бот запущен"

Проверьте работу:
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте, что бот отвечает

Если все работает, остановите бота: `Ctrl+C`

## 🔄 Шаг 7: Настройка автозапуска (systemd)

### 7.1 Создание systemd сервиса

```bash
sudo nano /etc/systemd/system/menstrual-tracker-bot.service
```

Вставьте следующее (замените `ваш_пользователь` на ваше имя пользователя):

```ini
[Unit]
Description=Menstrual Tracker Telegram Bot
After=network.target postgresql.service

[Service]
Type=simple
User=ваш_пользователь
Group=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/bots/menstrual_tracker_bot
Environment="PATH=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin"
ExecStart=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin/python3 /home/ваш_пользователь/bots/menstrual_tracker_bot/bot.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `ваш_пользователь` на реальное имя пользователя (можно узнать командой `whoami`)

### 7.2 Активация сервиса

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска при загрузке системы
sudo systemctl enable menstrual-tracker-bot

# Запуск сервиса
sudo systemctl start menstrual-tracker-bot

# Проверка статуса
sudo systemctl status menstrual-tracker-bot
```

### 7.3 Полезные команды для управления

```bash
# Статус сервиса
sudo systemctl status menstrual-tracker-bot

# Остановка
sudo systemctl stop menstrual-tracker-bot

# Запуск
sudo systemctl start menstrual-tracker-bot

# Перезапуск
sudo systemctl restart menstrual-tracker-bot

# Просмотр логов (последние 100 строк)
sudo journalctl -u menstrual-tracker-bot -n 100

# Просмотр логов в реальном времени
sudo journalctl -u menstrual-tracker-bot -f

# Отключение автозапуска
sudo systemctl disable menstrual-tracker-bot
```

## 🔍 Шаг 8: Проверка работы

1. **Проверка статуса:**
   ```bash
   sudo systemctl status menstrual-tracker-bot
   ```
   Должен быть статус `active (running)`

2. **Проверка логов:**
   ```bash
   sudo journalctl -u menstrual-tracker-bot -n 50
   ```
   Не должно быть ошибок

3. **Тест в Telegram:**
   - Найдите бота
   - Отправьте `/start`
   - Проверьте, что бот отвечает

## 🛠️ Обслуживание

### Резервное копирование

#### SQLite:
```bash
# Создание резервной копии
cp ~/bots/menstrual_tracker_bot/menstrual_tracker.db ~/backups/menstrual_tracker_$(date +%Y%m%d).db

# Автоматическое резервное копирование (добавить в crontab)
# crontab -e
# 0 2 * * * cp ~/bots/menstrual_tracker_bot/menstrual_tracker.db ~/backups/menstrual_tracker_$(date +\%Y\%m\%d).db
```

#### PostgreSQL:
```bash
# Создание резервной копии
pg_dump -U bot_user menstrual_tracker > ~/backups/menstrual_tracker_$(date +%Y%m%d).sql

# Восстановление
psql -U bot_user menstrual_tracker < ~/backups/menstrual_tracker_20260125.sql
```

### Обновление бота

```bash
# Остановка сервиса
sudo systemctl stop menstrual-tracker-bot

# Активация виртуального окружения
cd ~/bots/menstrual_tracker_bot
source venv/bin/activate

# Обновление кода (если используете git)
git pull

# Обновление зависимостей
pip install --upgrade -r requirements.txt

# Запуск сервиса
sudo systemctl start menstrual-tracker-bot

# Проверка статуса
sudo systemctl status menstrual-tracker-bot
```

## 🐛 Решение проблем

### Проблема: Бот не запускается

**Решение:**
1. Проверьте логи: `sudo journalctl -u menstrual-tracker-bot -n 100`
2. Проверьте токен в `.env` файле
3. Убедитесь, что виртуальное окружение активировано правильно
4. Проверьте права доступа к файлам

### Проблема: Ошибки базы данных

**Решение:**
1. Для SQLite: проверьте права доступа к файлу `.db`
2. Для PostgreSQL: проверьте, что сервис запущен: `sudo systemctl status postgresql`
3. Проверьте подключение: `psql -U bot_user -d menstrual_tracker`

### Проблема: Уведомления не приходят

**Решение:**
1. Проверьте, что уведомления включены в профиле пользователя
2. Проверьте настройки времени и часового пояса
3. Проверьте логи на наличие ошибок
4. Убедитесь, что планировщик задач работает

### Проблема: Бот не отвечает на команды

**Решение:**
1. Проверьте статус сервиса
2. Проверьте логи на ошибки
3. Убедитесь, что токен бота правильный
4. Проверьте подключение к интернету

## 📊 Мониторинг

### Настройка мониторинга логов

Создайте скрипт для мониторинга:

```bash
nano ~/check_bot.sh
```

```bash
#!/bin/bash
if ! systemctl is-active --quiet menstrual-tracker-bot; then
    echo "Бот не работает! Перезапускаю..."
    sudo systemctl restart menstrual-tracker-bot
fi
```

Сделайте исполняемым:
```bash
chmod +x ~/check_bot.sh
```

Добавьте в crontab (проверка каждые 5 минут):
```bash
crontab -e
# Добавьте строку:
*/5 * * * * /home/ваш_пользователь/check_bot.sh
```

## 🔐 Безопасность

1. **Защита .env файла:**
   ```bash
   chmod 600 ~/bots/menstrual_tracker_bot/.env
   ```

2. **Регулярные обновления:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   pip install --upgrade -r requirements.txt
   ```

3. **Резервное копирование:**
   - Настройте автоматическое резервное копирование базы данных
   - Храните резервные копии в безопасном месте

4. **Мониторинг:**
   - Регулярно проверяйте логи
   - Настройте алерты при ошибках

## ✅ Чек-лист развертывания

- [ ] Сервер Ubuntu подготовлен и обновлен
- [ ] Python 3.8+ установлен
- [ ] Бот создан в Telegram, токен получен
- [ ] Файлы проекта скопированы на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] Файл .env настроен с токеном
- [ ] База данных инициализирована
- [ ] Тестовый запуск прошел успешно
- [ ] Systemd сервис создан и настроен
- [ ] Сервис запущен и работает
- [ ] Бот отвечает в Telegram
- [ ] Настроено резервное копирование

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `sudo journalctl -u menstrual-tracker-bot -f`
2. Проверьте документацию в README.md
3. Убедитесь, что все шаги выполнены правильно

---

**Успешного развертывания! 🚀**
