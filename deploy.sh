#!/bin/bash

# Скрипт для автоматического развертывания бота на Ubuntu

echo "🚀 Начало развертывания бота для отслеживания менструального цикла..."

# Проверка Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python3 не установлен. Устанавливаю..."
    sudo apt update
    sudo apt install python3 python3-pip python3-venv -y
fi

# Создание директории проекта
PROJECT_DIR="$HOME/bots/menstrual_tracker_bot"
mkdir -p "$PROJECT_DIR"
cd "$PROJECT_DIR"

echo "📁 Директория проекта: $PROJECT_DIR"

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "🔧 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активация виртуального окружения
source venv/bin/activate

# Установка зависимостей
echo "📦 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Создание .env файла если его нет
if [ ! -f ".env" ]; then
    echo "⚙️ Создание файла .env..."
    cp .env.example .env
    echo "⚠️ ВАЖНО: Отредактируйте файл .env и укажите BOT_TOKEN!"
    echo "   Файл находится в: $PROJECT_DIR/.env"
fi

# Инициализация базы данных
echo "🗄️ Инициализация базы данных..."
python3 -c "from database import init_db; init_db()"

echo "✅ Развертывание завершено!"
echo ""
echo "📝 Следующие шаги:"
echo "1. Отредактируйте .env файл и укажите BOT_TOKEN"
echo "2. Запустите бота: python3 bot.py"
echo "3. Или настройте systemd сервис для автозапуска (см. README.md)"
