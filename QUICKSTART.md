# ⚡ Быстрый старт

## 🎯 Что было создано

Полнофункциональный Telegram-бот для отслеживания менструального цикла с:
- ✅ Сбором данных о цикле
- ✅ Расчетом текущей фазы
- ✅ Ежедневными уведомлениями с рекомендациями
- ✅ Справочником фаз цикла
- ✅ Настройкой уведомлений
- ✅ Профилем пользователя

## 📦 Файлы проекта

- `bot.py` - основной файл бота с логикой
- `database.py` - модели базы данных
- `cycle_calculator.py` - расчет фаз цикла
- `config.py` - конфигурация
- `requirements.txt` - зависимости
- `.env.example` - пример настроек
- `README.md` - основная документация
- `DEPLOYMENT_RU.md` - подробная инструкция по развертыванию

## 🚀 Быстрая установка на Ubuntu

### 1. Подготовка

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python
sudo apt install python3 python3-pip python3-venv -y
```

### 2. Создание бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен

### 3. Установка проекта

```bash
# Создание директории
mkdir -p ~/bots/menstrual_tracker_bot
cd ~/bots/menstrual_tracker_bot

# Копирование файлов (скопируйте все файлы проекта сюда)

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 4. Настройка

```bash
# Создание .env файла
cp .env.example .env
nano .env
```

Вставьте ваш токен:
```env
BOT_TOKEN=ваш_токен_здесь
DATABASE_URL=sqlite:///menstrual_tracker.db
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_NOTIFICATION_TIME=09:00
```

### 5. Запуск

```bash
# Инициализация базы данных
python3 -c "from database import init_db; init_db()"

# Запуск бота
python3 bot.py
```

## 🔧 Настройка автозапуска

Создайте файл `/etc/systemd/system/menstrual-tracker-bot.service`:

```ini
[Unit]
Description=Menstrual Tracker Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/home/ваш_пользователь/bots/menstrual_tracker_bot
Environment="PATH=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin"
ExecStart=/home/ваш_пользователь/bots/menstrual_tracker_bot/venv/bin/python3 /home/ваш_пользователь/bots/menstrual_tracker_bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация:
```bash
sudo systemctl daemon-reload
sudo systemctl enable menstrual-tracker-bot
sudo systemctl start menstrual-tracker-bot
sudo systemctl status menstrual-tracker-bot
```

## 📱 Использование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Заполните данные о цикле
4. Получайте ежедневные уведомления!

## 🐛 Решение проблем

**Бот не отвечает:**
- Проверьте токен в `.env`
- Проверьте логи: `sudo journalctl -u menstrual-tracker-bot -f`

**Ошибки базы данных:**
- Проверьте права доступа к файлу `.db`
- Переинициализируйте: `python3 -c "from database import init_db; init_db()"`

## 📚 Подробная документация

- `README.md` - общая информация
- `DEPLOYMENT_RU.md` - подробная инструкция по развертыванию
- `PROJECT_INFO.md` - информация о проекте

---

**Готово к использованию! 🎉**
